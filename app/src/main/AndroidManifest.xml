<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FarmNavi">
        <activity
            android:name=".Login_Activity"
            android:exported="false" />
        <activity
            android:name=".SignUp_Activity"
            android:exported="true">

        </activity>
        <activity
            android:name=".PestDiseaseAnalysisFragment"
            android:exported="false" />
        <activity
            android:name=".UserProfileFragment"
            android:exported="false" />
        <activity
            android:name=".MarketFragment"
            android:exported="false" />
        <!--
            Note: Since AdvisoryFragment is a Fragment and not a standalone Activity,
            this activity tag is likely incorrect. It should be removed if it's
            hosted by MainActivity.
        -->
        <activity
            android:name=".AdvisoryFragment"
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:exported="true" />
        <activity
            android:name=".Splash_Screen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OnBoarding_Screen"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>
